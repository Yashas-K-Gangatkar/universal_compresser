[package]
name = "yk_engine"
version = "0.2.0"
edition = "2021"
description = "YK Universal Compiler — two-engine compression system (Master Grid + 9-byte tickets)"
license = "MIT"
authors = ["Yashas Krishnamurthy"]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
